// Import the dependencies for testing
var chai     = require('chai')
var chaiHttp = require('chai-http');
var app      = require('../app.js');
// var db       = require('../DailyLyfe');

// Configure chai.
// chai.use(chaiHttp);
// chai.should();

describe("Calendar", function() {
    it('calendar shows correct month and year', function(done) {
        console.log('momo')
h               done();
    })
})